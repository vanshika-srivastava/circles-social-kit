'use client'
'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var _tslib = require('../../_virtual/_tslib.cjs');
var ethereumCore = require('@dynamic-labs/ethereum-core');
var getWeb3Provider = require('../getWeb3Provider/getWeb3Provider.cjs');

/**
 * Gets a signer for an ethereum wallet.
 *
 * If the wallet is NOT an Ethereum wallet, this will reject.
 */
const getSigner = (wallet) => _tslib.__awaiter(void 0, void 0, void 0, function* () {
    if (!ethereumCore.isEthereumWallet(wallet))
        throw new Error(`getSigner must be called with an Ethereum wallet, but wallet was of chain ${wallet.chain}`);
    const provider = yield getWeb3Provider.getWeb3Provider(wallet);
    return provider.getSigner(wallet.address);
});

exports.getSigner = getSigner;
