import { ReactElement } from 'react';
import { TypographyColor, TypographyWeight } from '../../../../components/Typography/Typography.types';
export type DotsMenuOption = {
    Icon: ReactElement | null;
    callback: VoidFunction;
    text: string;
    hide?: boolean;
    fontColor?: TypographyColor;
    fontWeight?: TypographyWeight;
    endSlot?: ReactElement;
    badge?: string;
};
type Props = {
    options: DotsMenuOption[];
    direction?: 'right' | 'left';
    buttonClassName?: string;
    buttonClassNameWithOpenMenu?: string;
    iconOverride?: ReactElement;
};
export declare const DotsMenu: ({ options, buttonClassName, buttonClassNameWithOpenMenu, direction, iconOverride, }: Props) => JSX.Element;
export {};
