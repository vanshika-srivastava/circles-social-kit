export type EmbeddedWalletSessionSettings = {
    emailCode: string;
    userId: string;
    createdAt: number;
};
export type EmbeddedWalletCurrentAction = 'signMessage' | 'sendTransaction' | 'helpSession' | 'reveal';
