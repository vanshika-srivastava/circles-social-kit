/// <reference types="react" />
import { TokenBalance } from '@dynamic-labs/sdk-api-core';
import { IUITransaction } from '@dynamic-labs/types';
export declare const TransactionCard: ({ transaction, currentToken, currencySymbol, valueRounded, showFiat, transactionValue, isGasSponsored, gasFiatValue, totalGasFiatValue, currency, total, t, walletKey, }: {
    transaction: IUITransaction;
    currentToken: TokenBalance | undefined;
    currencySymbol: string;
    valueRounded?: string;
    showFiat: boolean;
    transactionValue: string | undefined;
    isGasSponsored: boolean;
    gasFiatValue: string;
    totalGasFiatValue: string;
    currency: string;
    total: bigint | undefined;
    t: any;
    walletKey: string;
}) => JSX.Element;
