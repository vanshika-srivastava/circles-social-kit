'use client'
'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var jsxRuntime = require('react/jsx-runtime');
var React = require('react');
var classNames = require('../../utils/functions/classNames/classNames.cjs');

const ModalHeader = ({ alignContent, children, leading, displayLeading = true, trailing, displayTrailing = true, style, displayBorder, }) => {
    const hasLeading = displayLeading && leading && React.Children.count(leading);
    const hasTrailing = displayTrailing && trailing && React.Children.count(trailing);
    return (jsxRuntime.jsxs("div", { className: classNames.classNames('modal-header', {
            'modal-header--align-content-bottom': alignContent === 'bottom',
            'modal-header--border': displayBorder,
            'modal-header--empty': !hasLeading && !children && !hasTrailing,
        }), style: style, children: [displayLeading && (jsxRuntime.jsx("div", { className: classNames.classNames('modal-header__leading', {
                    'modal-header__leading--empty': !hasLeading,
                }), children: leading })), children, displayTrailing && (jsxRuntime.jsx("div", { className: classNames.classNames('modal-header__trailing', {
                    'modal-header__trailing--empty': !hasTrailing,
                }), children: trailing }))] }));
};

exports.ModalHeader = ModalHeader;
