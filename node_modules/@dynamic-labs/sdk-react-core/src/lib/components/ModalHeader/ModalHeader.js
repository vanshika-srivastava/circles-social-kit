'use client'
import { jsxs, jsx } from 'react/jsx-runtime';
import { Children } from 'react';
import { classNames } from '../../utils/functions/classNames/classNames.js';

const ModalHeader = ({ alignContent, children, leading, displayLeading = true, trailing, displayTrailing = true, style, displayBorder, }) => {
    const hasLeading = displayLeading && leading && Children.count(leading);
    const hasTrailing = displayTrailing && trailing && Children.count(trailing);
    return (jsxs("div", { className: classNames('modal-header', {
            'modal-header--align-content-bottom': alignContent === 'bottom',
            'modal-header--border': displayBorder,
            'modal-header--empty': !hasLeading && !children && !hasTrailing,
        }), style: style, children: [displayLeading && (jsx("div", { className: classNames('modal-header__leading', {
                    'modal-header__leading--empty': !hasLeading,
                }), children: leading })), children, displayTrailing && (jsx("div", { className: classNames('modal-header__trailing', {
                    'modal-header__trailing--empty': !hasTrailing,
                }), children: trailing }))] }));
};

export { ModalHeader };
