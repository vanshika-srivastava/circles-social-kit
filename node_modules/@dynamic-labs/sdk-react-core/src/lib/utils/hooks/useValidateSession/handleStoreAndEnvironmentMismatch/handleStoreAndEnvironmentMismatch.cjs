'use client'
'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var _tslib = require('../../../../../../_virtual/_tslib.cjs');
var environmentId = require('../../../../store/state/environmentId.cjs');

/**
 * Detects envId mismtach with store.
 * @returns true if there is no mismatch.
 */
const handleStoreAndEnvironmentMismatch = (_a) => _tslib.__awaiter(void 0, [_a], void 0, function* ({ environmentId: environmentId$1, logout, }) {
    const storeEnvId = environmentId.getEnvironmentId();
    if (!storeEnvId || environmentId$1 === storeEnvId) {
        return true;
    }
    yield logout('store and environment are out of sync', {
        currentEnvId: environmentId$1,
        storedEnvId: storeEnvId,
    });
    environmentId.setEnvironmentId(environmentId$1);
    return false;
});

exports.handleStoreAndEnvironmentMismatch = handleStoreAndEnvironmentMismatch;
