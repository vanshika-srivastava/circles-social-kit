import { UserProfile } from '../../../..';
type Props = {
    environmentId: string;
    user: UserProfile | undefined;
};
export declare const usePrimaryWalletId: ({ environmentId, user }: Props) => {
    clearPrimaryWalletId: () => void;
    primaryWalletId: string | undefined;
    setPrimaryWalletId: (walletId: string) => Promise<void>;
};
export {};
