import { UserProfile } from '@dynamic-labs/types';
import { AuthModeType } from '../../../../shared';
export declare const isConnectOnly: (user: UserProfile | undefined, authMode: AuthModeType) => boolean;
