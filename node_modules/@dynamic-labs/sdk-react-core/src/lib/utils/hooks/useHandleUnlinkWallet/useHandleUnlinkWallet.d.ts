import { JwtVerifiedCredential } from '@dynamic-labs/sdk-api-core';
import { Wallet } from '../../../shared';
type UseHandleUnlinkProps = {
    environmentId: string;
    primaryWalletId: string | undefined;
    secondaryWallets: Wallet[];
    verifiedCredentials: JwtVerifiedCredential[];
};
export declare const useHandleUnlinkWallet: ({ verifiedCredentials, environmentId, primaryWalletId, secondaryWallets, }: UseHandleUnlinkProps) => (walletId: string) => Promise<void>;
export {};
