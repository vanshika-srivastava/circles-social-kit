import { JsonRpcResponse } from './rpcSchema/jsonRpcResponse';
import { Observable } from './observable';
import { CirclesEvent } from './events/events';
export declare class CirclesRpc {
    private readonly rpcUrl;
    private idCounter;
    private websocket;
    private websocketConnected;
    private pendingResponses;
    private subscriptionListeners;
    constructor(rpcUrl: string);
    call<TResult>(method: string, params: any[]): Promise<JsonRpcResponse<TResult>>;
    private connect;
    private sendMessage;
    subscribe(address?: string): Promise<Observable<CirclesEvent>>;
}
//# sourceMappingURL=circlesRpc.d.ts.map