(function (factory) {
    if (typeof module === "object" && typeof module.exports === "object") {
        var v = factory(require, exports);
        if (v !== undefined) module.exports = v;
    }
    else if (typeof define === "function" && define.amd) {
        define(["require", "exports", "@hpke/core", "./hkdfSha256.js", "./x25519.js"], factory);
    }
})(function (require, exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.DhkemX25519HkdfSha256 = void 0;
    const core_1 = require("@hpke/core");
    const hkdfSha256_js_1 = require("./hkdfSha256.js");
    const x25519_js_1 = require("./x25519.js");
    /**
     * The DHKEM(X25519, HKDF-SHA256) for HPKE KEM implementing {@link KemInterface}.
     *
     * This class is implemented using
     * {@link https://github.com/paulmillr/noble-curves | @noble/curves}.
     *
     * The instance of this class can be specified to the
     * {@link https://deno.land/x/hpke/core/mod.ts?s=CipherSuiteParams | CipherSuiteParams} as follows:
     *
     * @example Use with `@hpke/core` (`https://deno.land/x/hpke/core/mod.ts`).
     *
     * ```ts
     * import {
     *   Aes128Gcm,
     *   CipherSuite,
     *   HkdfSha256,
     * } from "https://deno.land/x/hpke/core/mod.ts";
     * import { DhkemX25519HkdfSha256 } from "https://deno.land/x/hpke/x/dhkem-x25519/mod.ts";
     *
     * const suite = new CipherSuite({
     *   kem: new DhkemX25519HkdfSha256(),
     *   kdf: new HkdfSha256(),
     *   aead: new Aes128Gcm(),
     * });
     * ```
     *
     * When using `hpke-js` (`https://deno.land/x/hpke/mod.ts`), `KemId.DhkemX25519HkdfSha256`
     * can be used. You don't need to use this class.
     *
     * @example
     *
     * ```ts
     * import { AeadId, CipherSuite, KdfId, KemId } from "https://deno.land/x/hpke/mod.ts";
     *
     * const suite = new CipherSuite({
     *   kem: KemId.DhkemX25519HkdfSha256,
     *   kdf: KdfId.HkdfSha256,
     *   aead: AeadId.Aes128Gcm,
     * });
     * ```
     */
    class DhkemX25519HkdfSha256 extends core_1.Dhkem {
        constructor() {
            const kdf = new hkdfSha256_js_1.HkdfSha256();
            super(core_1.KemId.DhkemX25519HkdfSha256, new x25519_js_1.X25519(kdf), kdf);
            /** KemId.DhkemX25519HkdfSha256 (0x0020) */
            Object.defineProperty(this, "id", {
                enumerable: true,
                configurable: true,
                writable: true,
                value: core_1.KemId.DhkemX25519HkdfSha256
            });
            /** 32 */
            Object.defineProperty(this, "secretSize", {
                enumerable: true,
                configurable: true,
                writable: true,
                value: 32
            });
            /** 32 */
            Object.defineProperty(this, "encSize", {
                enumerable: true,
                configurable: true,
                writable: true,
                value: 32
            });
            /** 32 */
            Object.defineProperty(this, "publicKeySize", {
                enumerable: true,
                configurable: true,
                writable: true,
                value: 32
            });
            /** 32 */
            Object.defineProperty(this, "privateKeySize", {
                enumerable: true,
                configurable: true,
                writable: true,
                value: 32
            });
        }
    }
    exports.DhkemX25519HkdfSha256 = DhkemX25519HkdfSha256;
});
